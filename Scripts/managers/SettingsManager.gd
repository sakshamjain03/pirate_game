extends Node

# SettingsManager.gd
# Handles reading, writing, and applying game settings (audio, display, etc.).

func load_settings() -> void:
	print("Settings loaded.")

func save_settings() -> void:
	print("Settings saved.")
