[gd_scene load_steps=2 format=3 uid="uid://cabcde12345"]

[ext_resource type="Script" path="res://scripts/world/CameraRig.gd" id="1_c_rig"]

[sub_resource type="SphereShape3D" id="SphereShape3D_cam"]
radius = 1.5

[node name="CameraRig" type="Node3D"]
script = ExtResource("1_c_rig")

[node name="CameraPivot" type="Node3D" parent="."]
transform = Transform3D(1, 0, 0, 0, 0.669131, -0.743145, 0, 0.743145, 0.669131, 0, 0, 0)

; collision_mask 16 = layer 5 (terrain) only. It was 3 (ships + islands): since
; the rig rides at the target ship's origin, the arm's 1.5-radius sphere starts
; inside the ship's own collision shape, collides immediately, and collapses the
; arm to ~0 — putting the camera inside the hull. Terrain is the only thing the
; camera should actually pull in for.
[node name="SpringArm3D" type="SpringArm3D" parent="CameraPivot"]
shape = SubResource("SphereShape3D_cam")
spring_length = 30.0
collision_mask = 16

[node name="MainCamera" type="Camera3D" parent="CameraPivot/SpringArm3D"]
current = true
